import { HighlightSearchDirective } from './highlight -search.directive';

describe('HightlightSearchDirective', () => {
  it('should create an instance', () => {
    // const directive = new HightlightSearchDirective();
    // expect(directive).toBeTruthy();
  });
});
