import { FilterReportedUsersPipe } from './filter-reported-users.pipe';

describe('FilterReportedUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportedUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
